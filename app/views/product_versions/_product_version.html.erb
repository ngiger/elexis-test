<li>
<p>
  <b>Version:</b>
  <b><%= product_version.name %></b>
	<%= link_to 'Edit', edit_product_version_path(product_version) %> 
    <% if current_user.admin? %>
    | <%= link_to "delete", product_version, :method => :delete, :confirm => "You sure?",
    :title => "Delete #{product_version.name}" %>
    <% end %>
  <br><%= product_version.description %>
  <br><%= product_version.remarks %>
  <br>Erstellt von: <%= product_version.user_id %>
   deploy: <%= product_version.deploy_state_id %> 
<%= link_to 'Edit', edit_deploy_state_path(product_version.deploy_state_id) %> 
   plug-ins: <%= product_version.product_module_id %>
</li>
