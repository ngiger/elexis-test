%header
  = link_to logo, root_path
  %nav{:class=>"round"}
    %ul 
      = link_to "Home", root_path
      = link_to "Help", help_path
      = link_to "Users", users_path                           if  signed_in?()
      = link_to "Profile", current_user                       if  signed_in?()
      = link_to "Settings", edit_user_path(current_user)      if  signed_in?()
      = link_to "Sign out", signout_path, :method => :delete  if  signed_in?()
      = link_to "Sign in", signin_path                        if !signed_in?()
